# polly-autogen-gui プロジェクトドキュメント

> このファイルはプロジェクトフォルダ内のドキュメントです。このフォルダを開いた Claude セッション(別のMacやWindows/WSL2環境を含む)から常に参照できます。

## 目的

指定した `.txt` ファイル(1個、またはディレクトリ配下の全ファイル)を Amazon Polly で音声合成し、mp3(または選択した出力フォーマット)として出力する、Electronベースの GUI アプリ(Windows / Mac 対応)。

Python製CLIツール `polly-autogen` の後継として、同等の機能を GUI アプリとして再実装したもの。

## 現在の仕様(2026-09-13 時点)

### アーキテクチャ

Electron標準の3層構成:

- **main プロセス**(`src/main/main.js`) — ウィンドウ生成、ファイル/ディレクトリ選択ダイアログとPolly変換実行のIPCハンドラ
- **preload**(`src/main/preload.js`) — `contextBridge` 経由で `window.pollyAutogen` API を renderer に公開(`contextIsolation: true` / `nodeIntegration: false`)
- **renderer**(`src/renderer/`) — 素のHTML/CSS/JS(フレームワーク不使用)によるUI
- Polly呼び出し・テキスト分割・ファイルI/Oのコアロジックは `src/main/polly.js` に集約
- AWS SDKは `@aws-sdk/client-polly`(JS SDK v3)を使用
- 認証情報・リージョンはAWSのデフォルト認証チェーン(`~/.aws/credentials` / 環境変数など)に従う。GUI上にアクセスキー等の入力フォームは設けない方針

### UIの操作フロー

1. 「ファイルを選択」または「フォルダを選択」ボタンで対象を選ぶ(`dialog:select-file` / `dialog:select-directory` IPC)
2. オプションフォームで以下を設定:

   | オプション | ハードコードされたデフォルト | 備考 |
   | :---- | :---- | :---- |
   | Region | (空欄) | 空欄時はAWS SDKのデフォルト解決に従う |
   | Language Code | `ja-JP` | プルダウン。Amazon Polly `DescribeVoices` APIから動的取得した実在する言語のみ表示 |
   | Engine | `neural` | プルダウン。選択中のLanguage Codeで(いずれかの音声が)対応しているエンジンのみ表示(連動して変わる) |
   | Voice ID | `Tomoko` | プルダウン。選択中のLanguage Code **かつ** Engine の両方に対応する音声のみ表示(連動して変わる) |
   | Output Format | `mp3` | `mp3` / `ogg_vorbis` / `pcm` |

   **Language Code → Engine → Voice ID** の順に選択肢が絞り込まれる(存在しない組み合わせを選べないようにするため。この順序であれば、Language Code・Engineを決めた時点でVoice IDの候補が一意に定まる)。一覧はアプリ起動時に自動取得され、「音声一覧を再取得」ボタンで再取得できる(`polly:list-voices` IPC、`DescribeVoicesCommand`)。取得中/失敗時はログ欄に状況を表示し、取得できるまで「変換開始」は無効化される

   「音声一覧を再取得」の隣に「現在の音声設定を保存する」ボタンがある。押すと、その時点の Language Code / Engine / Voice ID / Output Format の組み合わせを設定ファイル(`<userData>/voice-settings.json`、`src/main/voiceSettings.js`)に保存する。

   起動時の初期値の決め方:
   - 設定ファイルが存在しない、または空/壊れている場合 → 上表のハードコードされたデフォルト(`ja-JP` / `neural` / `Tomoko` / `mp3`)を画面に適用し、その内容をそのまま設定ファイルへ保存する(初回起動時に自動でシードされる)
   - 設定ファイルが存在する場合 → その内容を読み込み、画面の各プルダウンに適用する(2回目以降の起動は保存済みの組み合わせがそのまま復元される)

   さらに、上記4項目のいずれかのプルダウンの現在値が保存済みの設定ファイルの値と異なる場合、そのプルダウンに赤いアウトライン(`outline`。`border`ではなくレイアウトに影響しない実装)を表示して知らせる(起動直後は読み込んだ値と一致しているため表示されず、その後ユーザーが手動で変更した場合にのみ不一致として表示される)
3. 「変換開始」ボタンで `polly:convert` IPCを呼び出す
4. 進捗は main → renderer へ `polly:progress` イベントで逐次通知(`start` / `file-start` / `file-done` / `file-error` / `done`)。ファイルごとの状態リストと全体の `[i/total]` 表示、ログ欄(エラー含む)をUIに表示する

### 出力

- 出力先: `<対象ファイルが置かれているディレクトリ>/mp3/<元のファイル名(拡張子なし)>.<拡張子>`
  - 拡張子は選択した Output Format に応じて `mp3` / `ogg` / `pcm` を使い分ける
- 同名ファイルが既にある場合は上書き
- ディレクトリモードでは、1ファイルの変換エラーで全体を止めず、そのファイルをエラー表示して次のファイルの変換を継続する

### 長文の扱い

CLI版と同様、Pollyの1リクエストあたりの文字数上限を踏まえ、2900文字を目安に句点・感嘆符・疑問符・改行の直後で自動的にテキストを分割してリクエストし、結果の音声データを結合して1つの音声ファイルとして出力する(`src/main/polly.js` の `splitText` / `synthesizeToAudio` / `convertTextFile`)。

## Excel取り込み機能(2026-09-13 実装済み)

### 目的

現状、TTS変換の入力は「すでに存在する`.txt`ファイル」に限られている。台本がExcelの「トークスクリプト」シートで管理されているケースに対応するため、Excelシートからページ単位のテキストを取り出し、目視確認・編集した上で`.txt`ファイル群として書き出す前段の機能を追加する。書き出した`.txt`ファイルは、既存のTTS変換フロー(フォルダモード)にユーザーが手動でそのままつなげる(今回のスコープでは自動連携はしない — 明示的に決定済み、[[結び付け方の決定]]参照)。

### メイン画面への追加

既存の「ファイルを選択」「フォルダを選択」(直接TTS変換用)はそのまま残し、新たに以下の2ボタンを持つセクションを追加する:

| ボタン | 用途 |
| :---- | :---- |
| Excelファイルを選択 | 初回。Excel(`.xlsx`)を選び、シートを解析してスプレッドシート編集画面を開く |
| 作業フォルダを開く | 2回目以降。既存の作業フォルダ(後述)を選び、その`text`サブフォルダの内容を読み込んでスプレッドシート編集画面を開く(Excelファイルは不要) |

### 「Excelファイルを選択」のフロー

1. ネイティブファイル選択ダイアログ(`.xlsx`/`.xls`フィルタ、単一選択)でExcelファイルを選ぶ
2. 選択したExcelファイルと同じ階層に、ファイル名(拡張子除く)と同名の作業フォルダを提案する
   - 同名のフォルダが既に存在する場合、既存の作業フォルダを誤って再利用・上書きしてしまわないよう、末尾に ` (2)`、` (3)`... と数字を付けて未使用の名前を探す(`resolveUniqueFolder`、`src/main/excelImport.js`)。存在しなければそのままの名前を使う
   - 決まった名前でこの時点でフォルダを作成しておく
3. 「作業フォルダを選択してください」というOKボタンのみの確認ダイアログを表示する(次に開くフォルダ選択ダイアログが何のためのものか分かるようにするため)
4. フォルダ選択ダイアログを、上記の提案フォルダ(連番済みの名前)を初期パスにして開き、ユーザーに確定(または別フォルダへの変更)をさせる
5. Excel内の「トークスクリプト」という名前のシートを探す。無ければエラーとしてログに表示し、処理を中断する
6. シートのA列(ページ番号)・F列(トークスクリプト)を読み取り、行データ `{ pageNumber, script }` の配列を作る
   - 1行目はヘッダー行とみなしてスキップする(A1/F1はデータとして扱わない)
   - A列・F列のどちらかが空の行はスキップする(データ不備として無視)
7. 読み取った行データを持ってスプレッドシート編集画面を開く(このときの`prefix`は、実際にユーザーが確定したフォルダ名(連番が付いていればそれを含む)を使う)

### 「作業フォルダを開く」のフロー(2回目以降)

1. フォルダ選択ダイアログで既存の作業フォルダを選ぶ
2. 選択したフォルダを解決する(`resolveWorkingFolder`、`src/main/excelImport.js`):
   - `<選択したフォルダ>/text` が存在する → 通常ケース。選択したフォルダをそのまま作業フォルダとして扱う
   - `<選択したフォルダ>/text` が存在せず、選択したフォルダ自体の名前が `text` である → **ユーザーが`text`フォルダ自体を選んでしまったケース**。一つ上の階層を作業フォルダとして自動的に読み替える(この場合も読み込み・保存とも正しい`text`フォルダに対して行われる)
   - どちらでもない → 該当なし。次のステップで「見つからない」旨の警告になる
3. 解決した作業フォルダの`<作業フォルダ>/text/`以下の`.txt`ファイル一覧を読み込む
   - それでも`<作業フォルダ>/text`が存在しない、または`.txt`が1件もない場合は、その旨をスプレッドシート画面のステータス欄・メイン画面のログの両方に警告として明示する(以前はサイレントに0件表示になっていた)
4. 各ファイル名 `<prefix>_<ページ番号>.txt` から、末尾の`_`以降・拡張子除いた部分をページ番号として復元する(prefixは解決後の作業フォルダ名。Excelファイルは不要)
5. ファイル名のパースに失敗したファイルはスキップし、警告を出す
6. 復元した行データを持ってスプレッドシート編集画面を開く(以降は「Excelファイルを選択」から開いた場合と同じ画面・同じ保存ロジックを共有する。保存も解決後の正しい作業フォルダに対して行われる)

### スプレッドシート編集画面

- 独立したウィンドウ(メイン画面とは別)。メイン画面の操作をブロックしない
- 画面上部に、メイン画面で保存されている音声設定(`voice-settings.json`)を簡素に表示する。例:「現在の Polly 出力設定:日本語(ja-JP)、neural、Tomoko」(言語名は`src/renderer/spreadsheet/spreadsheet.js`内の固定辞書`LANGUAGE_NAMES`で日本語化。未知の言語コードの場合はコードをそのまま表示)
- ページ番号列(読み取り専用)・トークスクリプト列・操作列(再生/生成ボタン)の3列をスプレッドシート形式で一覧表示する
- トークスクリプトのセルをクリックすると、テキストエリアを持つ編集ダイアログを開く。保存するとその行の内容がメモリ上のデータに反映される(この時点ではファイルへの書き込みはしない)
  - ページ番号セルはこの画面からは編集不可(Excel側の値をそのまま使う。ページ番号を直したい場合はExcel側を直して再取り込みする)
- 画面上部(または適切な位置)に「保存」ボタンを配置する。押すと、その時点の全行を対象に、`<作業フォルダ>/text/<prefix>_<ページ番号>.txt` へ**無条件に全件上書き**で書き出す(`text`フォルダが無ければ作成)
  - `prefix` は「Excelファイルを選択」経由なら Excelファイル名(拡張子なし)、「作業フォルダを開く」経由なら作業フォルダ名そのもの — どちらも実質的に作業フォルダ名と同じになる
  - 現在シートに存在しない(削除された)ページ番号に対応する、過去に書き出し済みの`.txt`ファイルは削除しない(残る)。もし将来「シートに無いファイルは掃除する」挙動が必要になった場合は別途仕様化する
- ウィンドウのサイズ・表示位置は、閉じる際(または移動・リサイズ時)に設定ファイルへ保存し、次回開いたときに復元する。設定ファイルはアプリの`userData`ディレクトリ配下に保存する(プロジェクトフォルダやExcelと同じ場所には置かない — 環境ごとの表示設定であり共有すべきものではないため)

#### 音声出力(mp3生成)

- 「保存」ボタンの隣に「一括音声出力」ボタンを配置する。ボタンの右側には「[現在の件数/全体件数]」形式の進捗表示(`#batch-progress`)を置く。押すと:
  1. その時点の全行をまず`text`フォルダへ保存する(保存ボタンと同じ処理を内部的に実行。表示中のテキストと生成される音声を必ず一致させるため)
  2. `<作業フォルダ>/mp3/`(`text`フォルダとは別の、作業フォルダ直下のフォルダ)を作成、既にあれば流用する
  3. 対象のいずれかの行に、書き出し先と同名の`.mp3`が既に存在する場合、上書き確認ダイアログ(OK/中止)を表示する。中止した場合は何も上書きせず処理を終了する(直前のテキスト保存は行われたままになる)
  4. 各行のトークスクリプトを、画面上部に表示されている音声設定(Language Code / Engine / Voice ID)でPolly音声合成し、`<作業フォルダ>/mp3/<prefix>_<ページ番号>.mp3` へ書き出す(Output Formatはメイン画面の設定に関わらず常に`mp3`固定。ボタン名・仕様上の記述が一貫して「mp3」であるため)
  5. 1件でもエラーが発生した場合、エラー内容を含むネイティブダイアログを表示し、「続行」「中止」をユーザーに選ばせる。「続行」なら次の行へ進み、「中止」ならそこで一括処理を打ち切る(それまでに成功した行のmp3はそのまま残る)。進捗表示・ステータス欄は各行の開始/完了/エラー、および中断した場合はその旨(「エラーのため一括音声出力を中断しました」)を逐次反映する
- ページ番号・トークスクリプトの各行に「再生」ボタンと「生成」ボタンを配置する
  - 「再生」ボタン: 対応する`<作業フォルダ>/mp3/<prefix>_<ページ番号>.mp3`が存在する場合のみ有効。クリックすると`<audio>`要素で再生する。存在しない場合はグレーアウト(無効化)する
  - 「生成」ボタン: 常に押せる。押すと、その時点の全行をまず`text`フォルダへ保存した上で、その行1件だけをPolly音声合成し、`mp3`フォルダ内の対応ファイルを上書き(無ければ新規作成)する。個別生成には上書き確認ダイアログは出さない(1件だけの明示的な操作のため)
- mp3の存在有無はスプレッドシート画面を開いた時点でチェックして反映する(`src/main/spreadsheetAudio.js`の`checkMp3Status`)。生成・一括生成が完了した行は、その場で再生ボタンが有効化される

### 今回のスコープ外(足りない部分として検討したが、あえて含めないもの)

- ページ行の追加・削除(Excelの行構成を正として扱う。スプレッドシート画面からの行追加・削除はサポートしない)
- ページ番号セルの編集(前述の通り読み取り専用)
- 重複ページ番号の検出・警告(重複があると同名ファイルを複数回上書きすることになり、最後に処理した行の内容が残る。実運用で問題になるようであれば警告表示を追加検討する)
- 保存後のTTS変換への自動連携(前述の通り、既存のフォルダモードにユーザーが手動でつなげる)
- 複数Excelファイルの一括取り込み(1回の操作につきExcel 1ファイル)

### 実装メモ

- `.xlsx`読み取りに `exceljs` を使用(`package.json`の`dependencies`に追加済み)
- コアロジックは `src/main/excelImport.js`(`parseTalkScriptSheet` / `loadRowsFromTextFolder` / `saveRowsToTextFolder`)
- スプレッドシートウィンドウの生成・状態保存は `src/main/spreadsheetWindow.js`(`openSpreadsheetWindow`)。ウィンドウ状態は `userData` 配下の `spreadsheet-window-state.json` に保存
- IPC: `dialog:select-excel-file` / `dialog:select-working-folder` / `excel:start-import` / `workfolder:start-review` / `spreadsheet:save`(main.jsで実装、preload.jsの`pollyAutogen`から`selectExcelFile` / `selectWorkingFolder` / `startExcelImport` / `startWorkFolderReview`として公開)
- スプレッドシート編集画面は `src/renderer/spreadsheet/`(`index.html` / `spreadsheet.js` / `style.css`)に独立したHTML/JS一式として実装。専用preloadは `src/main/preload-spreadsheet.js`(`window.spreadsheetAPI`)
- 編集ダイアログはネイティブHTML `<dialog>` 要素(`showModal()`)で実装。別ウィンドウやOSネイティブダイアログは使っていない

## PowerPointへの音声埋め込み機能(2026-09-13 実装済み)

### 目的

Excel取り込み→スプレッドシート編集→mp3生成の最終ステップとして、生成したmp3群をPowerPointの各スライドに「再生ボタン等の見た目を残さずに自動再生される音声」として埋め込み、スライドの自動送り時間を「音声再生時間+3秒」に設定する。

### メイン画面の追加セクション

既存の音声出力関連セクション群の直後、「ログ」セクションの直前に「PowerPointへの音声埋め込み」セクションを配置する。

| 要素 | 動作 |
| :---- | :---- |
| 「PowerPointファイルを選択」ボタン | `.pptx`のファイル選択ダイアログ。選択すると即座に`<同じ階層>/<ファイル名>_音声埋め込み.pptx`として処理用コピーを作成し(既存があれば上書き)、そのコピーのファイル名をボタンの隣に**赤文字**で表示する。元ファイルは一切変更しない |
| 「MP3フォルダを指定」ボタン | フォルダ選択ダイアログ(既存の`dialog:select-directory`を流用)。指定したパスを隣に表示する |
| 「音声を埋め込む」ボタン | 上記2つが揃った場合のみ有効。隣に`[現在/全体]`の進捗を表示する。完了・警告・エラーはメイン画面のログ欄に出力する |

### スライドとmp3の対応付け(ユーザーと合意済み)

**スライドの表示順序(何番目か)= mp3ファイル名末尾のページ番号**。3番目に表示されるスライドには`..._3.mp3`を埋め込む。ページ番号の取り出し規則は`text`フォルダの`.txt`と同じ(ファイル名末尾の`_`以降・拡張子除いた部分)。

- 表示順序は`ppt/presentation.xml`の`<p:sldIdLst>`と`ppt/_rels/presentation.xml.rels`を突き合わせて決定する(`slideN.xml`のファイル名の連番は並び替え後の表示順と一致しないことがあるため、ファイル名順では判断しない)
- 対応するmp3が無いスライド、対応するスライドが無いmp3、ページ番号を判別できないmp3は、それぞれスキップして警告を出す
- 1スライドの埋め込みエラーで全体を止めず、次のスライドへ進む(既存のTTS変換と同じ方針。一括音声出力のような続行/中止ダイアログは設けていない)

### 埋め込みの内部処理(`src/main/pptxEmbed.js`)

pptxはZIP+XML(OOXML)なので、`jszip`で展開し`@xmldom/xmldom`でXMLをDOM操作して書き戻す。各対象スライドに対して:

1. `music-metadata`でmp3の再生時間(秒)を取得
2. mp3本体を`ppt/media/audioEmbed<連番>.mp3`として追加。アイコン用の1x1透明PNG(コード内にbase64で埋め込み、外部アセット不要)を`ppt/media/pollyAudioIcon.png`として最初の1回だけ追加
3. `[Content_Types].xml`に`mp3`(audio/mpeg)・`png`(image/png)のDefault拡張子が無ければ追加
4. スライドの`_rels`に、`audio`(レガシー参照)・`media`(PowerPoint 2010+の`p14:media`用)・`image`(アイコン)の3つのリレーションシップを追加(既存の`rId`と衝突しない番号を採番)
5. スライドXMLの`<p:spTree>`末尾に音声オブジェクト`<p:pic>`を追加。`hidden="1"`(PowerPointの「スライドショーを実行中にサウンドのアイコンを隠す」に相当)かつ極小サイズ・透明画像なので、再生ボタン等の見た目は一切出ない
6. スライドに既存の`<p:timing>`が**無い場合のみ**、自動再生用の`<p:timing>`を追加(`presetClass="mediacall"`+`nodeType="withEffect"`+`cmd="playFrom(0.0)"`と`<p:audio>/<p:cMediaNode>`によるスライド開始時自動再生)。既に`<p:timing>`がある場合はマージによる破損を避けて追加をスキップし、警告を出す(音声自体は埋め込まれるので、自動再生はPowerPoint側で手動設定可能)
7. `<p:transition>`の`advTm`(自動送り、ミリ秒)を`再生時間+3000`に設定(既存の`<p:transition>`があれば`advTm`のみ更新)

出力フォーマットはこれまで同様mp3のみを対象とする。

### 実機PowerPointでの確認が必要(重要)

この開発環境には実機のPowerPoint/Keynoteが無いため、生成したpptxを実際に開いての確認はできていない。ZIP構造・全XMLパートの整形式・リレーションシップの解決・`advTm`の値までは機械的に検証済みだが、PowerPoint側のスキーマ検証(特に自動再生用`<p:timing>`の受け入れ)は**ユーザーが実機で開いて確認する必要がある**。万一「修復が必要」と表示された場合でも、PowerPointの修復機能はタイミング部分のみを除去して音声埋め込み自体は残すことが多い(その場合は自動再生を手動設定する)。

## 経緯・決定事項

- 元々はPython製CLIツール `polly-autogen` として存在(ディレクトリ/ファイルモード、`--voice-id` 等のオプション、長文自動分割の仕様はそのCLI版を踏襲)。それをElectronベースのGUIアプリ `polly-autogen-gui` として再実装した
- GUI版ではAWS認証情報の入力フォームを設けず、AWS CLI/SDKのデフォルト認証チェーンに委ねる方針とした(CLI版の思想を踏襲し、スコープ拡大を避けるため)
- 出力フォーマットの選択に応じて出力ファイルの拡張子を切り替える(`mp3`/`ogg`/`pcm`)よう、CLI版の「常に`.mp3`」から仕様を拡張した
- ディレクトリモードは1ファイルのエラーで全体を止めず、バッチとして処理を継続する方針とした(GUIのバッチツールとしての挙動を優先)
- Voice ID / Language Code / Engine は当初テキスト入力・固定選択肢だったが、存在しない組み合わせを選んでAPIエラーになるのを防ぐため、Amazon Polly `DescribeVoices` APIから動的に取得したデータで連動プルダウンに変更した。ハードコードした音声一覧を持たない方針(APIの音声追加/変更に追従できるため)
- 連動プルダウンの絞り込み順は当初 Language Code → Voice ID → Engine だったが、これだと「特定の音声を選んだ後にその音声が対応するエンジンを選ぶ」という順序になり不自然だった。Language Code → Engine → Voice ID の順に変更し、「言語とエンジンを決めればVoice IDの候補が一意に定まる」形にした(ユーザーからの指摘・要望)
- Excel取り込み機能の「2回目以降」の入力(処理済みフォルダ)は、既存のTTS変換フローへ直接渡すのではなく、スプレッドシート編集画面を再度開く(Excel無しで再編集できるようにする)方針とした
- Excelから書き出した`.txt`(textフォルダ)と既存のTTS変換(Polly変換)は、あえて自動連携させず、別々の手動ステップのままとする方針とした(既存のフォルダモード変換にユーザーが手動でつなげる。スコープを広げすぎないため)
- Excel選択後にいきなりネイティブのフォルダ選択ダイアログが開くと何のためのものか分かりにくいとのフィードバックを受け、その前に「作業フォルダを選択してください」というOKのみの確認ダイアログを挟むようにした
- 「作業フォルダを開く」で`text`フォルダが見つからない/空だった場合、以前はサイレントに「0件」表示になり原因が分かりづらかった(実際に、`text`フォルダ自体を作業フォルダとして選んでしまうと何も読み込まれない、という報告があった)。フォルダが無い場合・`.txt`が1件も無い場合の両方で理由を明示する警告を出すよう修正した
- さらに、「`text`フォルダ自体を選んでしまう」ケースは警告を出すだけでなく、実際に読み込み・保存の両方が正しく動くように自動補正する方針に変更した(選択したフォルダに`text`という名前が付いていて、かつ`<選択>/text`が存在しない場合は、一つ上の階層を作業フォルダとして扱う)。ユーザーからの追加要望を受けての対応
- 「Excelファイルを選択」時に提案する作業フォルダが既に存在する場合、既存の作業フォルダ(過去の編集内容を含む可能性がある)を誤って再利用してしまわないよう、末尾に` (2)`、` (3)`...と連番を付けて回避する方針とした。ユーザーからの明示的な要望
- 音声設定(Language Code/Engine/Voice ID/Output Format)を設定ファイルに保存し、現在の選択が保存済みの設定とずれている場合にプルダウンへ視覚的な警告を出す機能を追加した。視覚的な警告は`border`ではなく`outline`(`outline-offset: -1px`)で実装し、レイアウトのサイズ・位置に一切影響しないようにした(ユーザーからの明示的な指定)
- 上記の音声設定は、当初は「保存済みの設定と一致しているかを知らせるだけ(自動適用はしない)」という方針だったが、その後「初回起動時のデフォルトは`ja-JP`/`neural`/`Tomoko`とし、設定ファイルが無い/空ならそれを保存する。2回目以降の起動では設定ファイルを読み込んで画面に反映する」という要望に変更された。これにより、設定ファイルは単なる比較用ではなく、実際に起動時の初期値を決めるものになった。Voice IDのハードコードされたデフォルトも`Takumi`から`Tomoko`に変更した
- スプレッドシート編集画面にmp3生成機能(一括音声出力・行ごとの個別生成・再生)を追加した。音声合成に使うPolly設定は、メイン画面の「音声設定保存」機能で保存された`voice-settings.json`をスプレッドシート画面を開く時点で読み込んで利用する(スプレッドシート画面自体には音声設定の変更UIは置かない)。出力フォーマットはユーザーの指示文言(「mp3フォルダ」「テキストファイル名.mp3」等)に合わせて常に`mp3`固定とし、メイン画面のOutput Format設定には連動させない
- mp3の出力先は`<作業フォルダ>/mp3/`(`text`フォルダと同階層のサブフォルダ)とした。既存の`convertTextFile`(`src/main/polly.js`)は入力ファイルと同階層に`mp3`フォルダを作る仕様のため、そのまま使うと`<作業フォルダ>/text/mp3/`になってしまい要件と異なる。そのため専用の書き出し関数(`synthesizeRowToMp3`、`src/main/spreadsheetAudio.js`)を新設し、`synthesizeToAudio`(`polly.js`から新たにexport)のみを再利用する形にした
- 一括音声出力は、まずテキストを保存してから音声化する(表示中のテキストと生成される音声が必ず一致するようにするため)。個別生成も同様に保存してから対象行のみ音声化する。上書き確認ダイアログは一括音声出力のみに表示し(複数ファイルへの破壊的操作のため)、個別生成には表示しない(1件だけの明示的操作のため)方針とした
- 一括音声出力でエラーが起きた場合の挙動は、他のTTS変換(直接txt変換のディレクトリモード)とは異なり、「1件のエラーで自動的に次へ進む」のではなく、エラー内容を示すダイアログを都度表示し、続行するか中止するかをユーザーに選ばせる方針にした(ユーザーからの明示的な要望)。あわせて、ボタン横に`[現在/全体]`の進捗表示を追加した
- PowerPoint音声埋め込みは、既製のpptx操作ライブラリ(`pptx-automizer`等)が「既存スライドへの音声追加+アイコン非表示+自動再生+自動送り」をサポートしていないことを確認したため、`jszip`+`@xmldom/xmldom`でOOXMLを直接操作する方式とした。文字列置換ではなくDOM操作にしたのは、既存のスライド構造を壊さず要素を追加するため
- PowerPoint音声埋め込みで、既存の`<p:timing>`(アニメーション)を持つスライドには自動再生タイミングを追加しない(スキップして警告)方針とした。マージを試みて既存アニメーションごと壊すリスクより、音声だけ埋め込んでユーザーに手動設定してもらう方が安全なため
- PowerPoint音声埋め込みの処理用コピー(`_音声埋め込み.pptx`)は、Excel作業フォルダのような連番回避(` (2)`)は行わず毎回上書きする。「選択したら処理用コピーを作る」という指示から、同じ元ファイルを選び直したときは常に新しいコピーで処理し直す用途と判断したため
- UIが殺風景だったため、Windows 11(Fluent Design / WinUI 3)の標準的な見た目に寄せて刷新した(`src/renderer/style.css`・`src/renderer/spreadsheet/style.css`をほぼ全面書き換え)。方針: 動作ロジック(JS・要素のid/クラス名)には一切手を入れず、CSSと最小限のHTML(音声出力関連の4セクションを`.card`でまとめるラッパー追加)のみで実現。トークン: 背景`#f3f3f3`のレイヤー、白いカード(角丸8px・薄い枠線と影)、アクセント色`#005fb8`のプライマリボタン(変換開始/音声を埋め込む/保存/一括音声出力)、入力欄は下辺を強調しフォーカス時にアクセント色、進捗表示はピル(丸バッジ)型、ログはダークコンソール風。フォントは`Segoe UI Variable`→`Segoe UI`→macOSシステムフォント→日本語フォントの順でフォールバック。ライトテーマのみ(ダークモードは未対応、意図的にスコープ外)
- 刷新にあたり、レイアウトを崩さない実装を維持: 音声設定の不一致警告は引き続き`outline`(要素サイズに影響しない)、フォーカス時の下線強調も`border`幅の変更ではなく`box-shadow: inset`で実現(1pxのレイアウトずれを防ぐため)。空の進捗/ステータス要素は`:empty { display: none }`で非表示にし、余分な余白が出ないようにした。メインウィンドウの初期サイズはカード化に伴い720x560→840x820に拡大し、`backgroundColor`を背景色と合わせて起動時の白フラッシュを防止
- 上記840x820では実際のコンテンツ高さ(全セクション分、約960px)に対してウィンドウの内部表示領域が足りず、ログ欄が見切れる不具合が発生した。ウィンドウサイズを880x1000(`minWidth: 720` / `minHeight: 560`)に拡大して解消した。あわせて、将来さらにセクションが増えても内容が見えなくなることがないよう、`html`に`height:100%`、`body`に`height:100%; overflow-y:auto`を設定し、ウィンドウより内容が高くなった場合は自動でスクロールできるようにした(保険的な対応)

## ファイル構成

- `src/main/main.js` — Electronメインプロセス(ウィンドウ生成、IPCハンドラ)
- `src/main/preload.js` — `contextBridge` でメインウィンドウのrendererにAPIを公開
- `src/main/polly.js` — Polly呼び出し・テキスト分割・ファイルI/Oのコアロジック
- `src/main/excelImport.js` — Excel解析・作業フォルダのテキストファイル読み書き
- `src/main/spreadsheetWindow.js` — スプレッドシート編集ウィンドウの生成・状態保存
- `src/main/preload-spreadsheet.js` — スプレッドシート編集ウィンドウ用の`contextBridge`
- `src/main/voiceSettings.js` — 音声設定(Language Code/Engine/Voice ID/Output Format)の保存・読み込み
- `src/main/spreadsheetAudio.js` — スプレッドシート画面向けのmp3生成・存在チェック(`<作業フォルダ>/mp3/`への書き出し)
- `src/main/pptxEmbed.js` — PowerPoint(pptx/OOXML)への音声埋め込み(処理用コピー作成・表示順スライド解決・音声オブジェクト/自動再生タイミング/自動送り時間の書き込み)。依存: `jszip` / `@xmldom/xmldom` / `music-metadata`(いずれも`dependencies`に追加済み)。テスト用pptx生成のため`pptxgenjs`を`devDependencies`に追加
- `src/renderer/index.html` — メイン画面UI
- `src/renderer/renderer.js` — メイン画面のUIロジック(IPC呼び出し、進捗・ログ表示)
- `src/renderer/style.css` — メイン画面のスタイル
- `src/renderer/spreadsheet/index.html` / `spreadsheet.js` / `style.css` — スプレッドシート編集画面一式
- `package.json` — 依存関係、`electron-builder` 設定(mac: dmg, win: nsis)
- `PROJECT.md` — 本ファイル

README.md / INSTALL.md 等のセットアップ手順ドキュメントはまだ未作成。

## 動作確認状況(2026-09-13 時点)

以下は確認済み:

- テキスト分割ロジック(`splitText`)をNode.js単体で実行し、2900文字境界での分割・再結合の正しさを確認済み
- `electron .` の起動でメインプロセスがエラーなく起動し、IPCハンドラが正しく登録されることを確認済み
- 実際のウィンドウ描画をスクリーンショットで確認済み(設計通りのレイアウト)
- 実際にAWS認証情報(`~/.aws/credentials`)を設定した上で、実際のAmazon Polly呼び出しによる変換をエンドツーエンドで確認済み。正しいmp3ファイル(音声データ)が生成されることを`afinfo`で確認
- Language Code → Voice ID → Engine の連動プルダウンが、実際のPolly APIから取得した106件の音声データで正しく絞り込まれることを確認済み
- Excel取り込み機能一式をエンドツーエンドで確認済み: 「トークスクリプト」シート(A列ページ番号・F列スクリプト、ヘッダー行・空行の除外を含む)の解析、シート名不一致時のエラー、Excelファイル選択→「作業フォルダを選択してください」確認ダイアログ→同名作業フォルダの自動作成→フォルダ選択ダイアログ(いずれもネイティブダイアログをOS操作で確定)→スプレッドシート画面表示、セルクリックでの編集ダイアログとメモリ上への反映、保存ボタンによる`<作業フォルダ>/text/<prefix>_<ページ番号>.txt`への書き出し・既存ファイルの上書き、「作業フォルダを開く」による`text`フォルダからのページ番号・スクリプト復元、`text`フォルダが無い/空の場合に警告が表示されること(誤って`text`フォルダ自体を作業フォルダとして選んだケースを含む)、スプレッドシートウィンドウのリサイズ・移動後の状態(`spreadsheet-window-state.json`)保存と次回起動時の復元
- ネイティブのファイル/フォルダ選択ダイアログ・OKダイアログの操作も、自動化シェルからOSレベルのキー操作で実際に確定させて確認済み(Chromiumの中のボタンをAppleScriptで直接クリックすることはできないが、ネイティブダイアログ自体は操作可能。詳細はmemory参照)
- スプレッドシート画面の音声設定表示・mp3生成機能一式をエンドツーエンドで確認済み: 起動時のヘッダー表示(実際のPolly設定に基づく「日本語(ja-JP)、neural、Tomoko」表示)、個別「生成」ボタンによる単一行の音声化(`<作業フォルダ>/mp3/<prefix>_<ページ番号>.mp3`への書き出し、`afinfo`で正当なmp3であることを確認)とその行の再生ボタンの即時有効化、「一括音声出力」による全行分の音声化(既存mp3がある場合のネイティブ上書き確認ダイアログを実際にOS操作でOK/中止の両方試し、中止時はファイルの`mtime`が変化しないこと・OK時は正しく上書きされることを確認)、再生ボタンでの実際の音声再生(`<audio>`要素、`file://`URL、`duration`が正しく取得できファイルが再生開始されることを確認)
- 一括音声出力のエラー時ダイアログ・続行/中止の分岐、および進捗表示(`[現在/全体]`)を確認済み: わざと存在しない音声IDを指定して全行を失敗させ、エラーダイアログが表示されること、「続行」を選び続けると全件(失敗のまま)試行して「完了しました」表示になること、「中止」を選ぶと1件目で処理を打ち切り「中断しました」表示になること、進捗表示が中断時点(`[1/3]`)を正しく示すことを確認

- PowerPoint音声埋め込みを、実機PowerPointが無い環境で可能な範囲までエンドツーエンドで確認済み: `pptxgenjs`で生成した4スライドのテストpptxとPollyで生成した実mp3(ページ1〜3、および対応スライドの無いページ9)を用い、UI上の実際のボタン操作(ネイティブのファイル/フォルダ選択ダイアログを「フォルダへ移動」経由でOS操作して確定)→処理用コピー作成と赤文字表示→埋め込み実行→進捗`[4/4]`→ログへの完了(3件)・警告(スライド4にmp3なし/ページ9にスライドなし)出力までを確認。生成物に対し、mp3・アイコンがZIP内に元のサイズで存在すること、32個のXML/relsパート全てが`xmllint`で整形式であること、各スライドの音声オブジェクト(`hidden="1"`)・自動再生`<p:timing>`・リレーションシップの解決・`advTm`が`music-metadata`で取得した再生時間+3000msと厳密一致することを確認

以下は未確認:

- **PowerPoint音声埋め込みの生成物を実機のPowerPointで開いての動作確認**(この環境にPowerPoint/Keynoteが無いため)。上記の構造検証は整形式・参照整合性までであり、PowerPointのスキーマ検証(特に自動再生タイミングの受け入れ)と実際の自動再生・自動送りの動作はユーザー側での確認が必要
- 実際にマウスでページ内のボタン(「ファイルを選択」等)をクリックする操作そのもの(自動化シェルからはChromiumが既定でアクセシビリティツリーを公開しないため、ページ内要素をAppleScriptで直接クリックすることはできない。ユーザー側での操作確認が必要)

## Gotchas(ハマりどころ)

- **Electronの永続プロファイルによる `<select>` 値の復元**: `userData` ディレクトリ(プロファイル)が同じ状態でアプリを再起動すると、Chromiumが前回セッションで選択されていた `<select>` の値を、JS側で設定したデフォルト値より後に復元してしまうことがある(症状: 起動のたびにVoice IDのデフォルトがTakumiではなく別の値になる、非決定的)。対策として `language-code` / `voice-id` / `engine` の `<select>` に `autocomplete="off"` を付与して回避している([index.html](src/renderer/index.html))。
